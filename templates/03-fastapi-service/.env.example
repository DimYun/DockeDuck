# ─── FastAPI service settings ─────────────────────────────────────────────────
# Copy this file to .env. The base template needs no secrets to run; add your own
# app config below (DB URLs, API keys, …) as your service grows.
APP_HOST=0.0.0.0
APP_PORT=8000
LOG_LEVEL=info
# DATABASE_URL=
# SECRET_KEY=
